# 实战01_代码备份

## main

```
package 实战01;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.Statement;
import java.util.Scanner;

public class 学生管理模块 {	

	public static void main(String[] args) { 
		String url = "jdbc:mysql://localhost:3306/实战01?serverTimezone=GMT";
		String user = "root";
		String password = "20061126hys"; 
		String driver = "com.mysql.cj.jdbc.Driver";
		Scanner scan = new Scanner(System.in);
		Connection conn = null;
		Statement stmt = null;
		ResultSet rs = null;
		try {
			Class.forName(driver);
			conn = DriverManager.getConnection(url, user, password);
			stmt = conn.createStatement();
			boolean bol = true;
			tool t = new tool();
			while(bol) {
				System.out.println(
						"\n=============================\n"+
						"学生管理模块\n" + 
						"1.添加新班\n" + 
						"2.新生录入\n" + 
						"3.按照班级查询学生\n" + 
						"4.统计每个班级学生总人数\n" + 
						"5.按照学生编号对学生降班\n" + 
						"6.按照学生编号对学生休学\n"+
						"7.按照学生编号修改学生信息\n"+
						"8.按照学生编号对学生复课\n"+
						"9.按照学生编号可以查看每个学生的详细学习变动情况\n"+
						"10.统计所有学生中事最多的学生\n"+
						"11.根据编号调整某个学生的专业\n"+
						"12.学生可以查看自己每个科目的成绩\n"+
						"13.学生可以根据学号统计自己的一个学期的总成绩\n"+
						"14.学生可以根据学号统计自己的一个学期的平均成绩\n"+
						"15.学生可以根据学号查看自己一个学期的总绩点\n"+
						"16.退出系统");
				System.out.println("请您输入功能序号选择功能");
				int i = scan.nextInt();
				if(i == 1) {
					t.addClass(stmt);
				}else if(i == 2) {
					t.showClasses(stmt);
					t.addStudent(stmt);
				}else if(i == 3) {
					t.showClasses(stmt);
					t.selectStudentByClass(stmt);
				}else if(i == 4) {
					t.showClasses(stmt);
					t.StudentNumOfClass(stmt);
				}else if(i == 5) {
					t.showStudents(stmt);
					t.jb(stmt);
				}else if(i == 6) {
					t.showStudents(stmt);
					t.stopStudy(stmt);
				}else if(i == 7) {
					t.showStudents(stmt);
					t.changStudent(stmt);
				}else if(i == 8) {
					t.showStudents(stmt);
					t.resumeStudy(stmt);
				}else if(i == 9) {
					t.showStudents(stmt);
					t.showStudentChange(stmt);
				}else if(i == 10) {
					t.theMostChangeStudent(stmt);
				}else if(i == 11) {
					t.showStudents(stmt);
					t.updateZy(stmt);
				}else if(i == 12) {
					t.showStudents(stmt);
					t.showScore(stmt);
				}else if(i == 13) {
					t.showStudents(stmt);
					t.showsumScore(stmt) ;
				}else if(i == 14) {
					t.showStudents(stmt);
					t.showAvgScore(stmt);
				}else if(i == 15) {
					t.showStudents(stmt);
					t.sumPoint(stmt);
				}else if(i == 16) {
					bol = false;
					System.out.println("谢谢使用!");
				}else {
					System.out.println("本系统目前只有17个功能静待升级!");
				}
				
			}
			
		}catch(Exception e) {
			e.printStackTrace();
		}finally {
	        try {
	            if(rs != null) rs.close();
	            if(stmt != null) stmt.close();
	            if(conn != null) conn.close();
	        } catch (Exception e) {
	            e.printStackTrace();
	        }
		}
    }
	
}

```





## 工具类

```
package 实战01;

import java.sql.ResultSet;
import java.sql.Statement;
import java.util.Scanner;

public class tool {
	Scanner scan = new Scanner(System.in);
	
	
	public void showClasses(Statement stmt) throws Exception {
		String sql = "SELECT * FROM class";
		ResultSet rs = stmt.executeQuery(sql);
		System.out.println("班级编号\t班级名称");
		while (rs.next()) {
		    System.out.println(rs.getInt("cid") + "\t" + rs.getString("cname"));
		}
		
	}
	public void showStudents(Statement stmt) throws Exception {
		String sql = "SELECT * FROM student";
		ResultSet rs = stmt.executeQuery(sql);
		System.out.println("编号\t姓名\t班级编号\t专业\t\t状态");
		while (rs.next()) {
			int zt = rs.getInt("zt");
        	String zt2;

        	if (rs.wasNull() || zt == 0) {
        		zt2 = "休学";
        	}else{
        		zt2 = "在读";
        	}
        	System.out.println(rs.getInt("sid") + "\t" + rs.getString("sname") + "\t" + rs.getInt("sid")+"\t"+ rs.getString("zy") + "\t\t" + zt2);
		}
		
	}
	public void addClass(Statement stmt) throws Exception {
            System.out.println("输入班级名(格式：x年x班)：");
            String cname = scan.next();
            String sql = "INSERT INTO class(cname) VALUES('" +cname+"')";
            stmt.executeUpdate(sql);
            System.out.println("添加完成");
       
    }

	public void addStudent(Statement stmt) throws Exception{
		System.out.println("输入姓名：");
        String sname = scan.next();
        System.out.println("输入班级编号：");
        int cid = scan.nextInt();
        System.out.println("输入专业：");
        String zy = scan.next();
        String sql = "INSERT INTO student(sname,cid,zy) VALUES('" + sname + "'," + cid + ",'" + zy + "')";
        stmt.executeUpdate(sql);
        System.out.println("添加完成");
	}
	
	public void selectStudentByClass(Statement stmt) throws Exception {
        System.out.println("输入班级编号：");
        int cid = scan.nextInt();
        String sql = "SELECT * FROM student WHERE cid='"+cid+"'";
        ResultSet rs = stmt.executeQuery(sql);
        System.out.println("编号\t\t姓名\t\t专业\t\t\t状态");

        while (rs.next()) {
        	int zt = rs.getInt("zt");
        	String zt2;

        	if (rs.wasNull() || zt == 0) {
        		zt2 = "休学";
        	}else{
        		zt2 = "在读";
        	}
        	System.out.println(rs.getInt("sid") + "\t\t" + rs.getString("sname") + "\t\t" + rs.getString("zy") + "\t\t\t" + zt2);
        }
	}
        
        
    public void StudentNumOfClass(Statement stmt) throws Exception {
        
    	String sql = "SELECT c.cname,COUNT(s.sid) num FROM class c LEFT JOIN student s ON c.cid=s.cid GROUP BY c.cid";
    	ResultSet rs = stmt.executeQuery(sql);
        while (rs.next()) {
            System.out.println(rs.getString("cname")+"："+rs.getInt("num")+"人");
        }
    }       
    
    public void jb(Statement stmt) throws Exception {
    	System.out.println("请输入要降班的学生编号：");
    	int sid = scan.nextInt();
    	String sql = "SELECT s.sname, s.zy, s.zt, s.cid old_cid,SUBSTRING_INDEX(c.cname,'年',1)+0 grade FROM student s JOIN class c ON s.cid=c.cid WHERE s.sid = "+sid+"";
    	ResultSet rs = stmt.executeQuery(sql);
    	if (!rs.next()) {
            System.out.println("错误：未找到编号为 "+sid+" 的学生！");
            return;
        }
    	String sname = rs.getString("sname");
        String zy = rs.getString("zy");
        int zt = rs.getInt("zt");
    	int old_cid = rs.getInt("old_cid");
    	int now_grade = rs.getInt("grade");
    	System.out.println("当前班级编号：" + old_cid + "  年级：" + now_grade);
    	String sql2 = "SELECT cid, cname, SUBSTRING_INDEX(cname,'年',1)+0 grade FROM class WHERE SUBSTRING_INDEX(cname,'年',1)+0 < "+now_grade+"  ORDER BY grade DESC"; 
    	ResultSet rs2 = stmt.executeQuery(sql2);
        System.out.println("\n===== 可降入班级列表 =====");
        System.out.println("班级编号\t班级名称\t年级");
        boolean hasClass = false;
        while (rs2.next()) {
            hasClass = true;
            System.out.println(rs2.getInt("cid") + "\t" + rs2.getString("cname") + "\t" + rs2.getInt("grade"));
            
        }
        if (!hasClass) {
            System.out.println("已是最低年级，无法降班！");
        }
        System.out.println("请输入要降入的班级编号：");
        int new_cid = scan.nextInt();
        String sql3 = "UPDATE student SET cid="+new_cid+" WHERE sid="+sid+"";
        stmt.executeUpdate(sql3);
        
        String sql4 = "INSERT INTO student_change(sid,old_cid,new_cid,old_sname,new_sname,old_zy,old_zt,new_zt) VALUES(" +sid+","+old_cid+","+new_cid+",'"+sname+"','"+sname+"','"+zy+"',"+zt+","+zt+")";
        stmt.executeUpdate(sql4);
        System.out.println("降班完成");
    }
    
    
    public void stopStudy(Statement stmt) throws Exception {
    	System.out.println("请输入休学学生编号：");
        int sid = scan.nextInt();
        String sql = "SELECT * FROM student WHERE sid="+sid+"";
		ResultSet rs = stmt.executeQuery(sql);
		if (!rs.next()) {
            System.out.println("错误：未找到编号为 "+sid+" 的学生！");
            return;
        }
		int cid = rs.getInt("cid");
        String sname = rs.getString("sname");
        String zy = rs.getString("zy");
        int old_zt = rs.getInt("zt");
        String sql2 = "UPDATE student SET zt=NULL WHERE sid="+sid+"";
        stmt.executeUpdate(sql2);
        
        String sql3 = "INSERT INTO student_change(sid,old_cid,new_cid,old_sname,new_sname,old_zy,old_zt,new_zt) VALUES(" +sid+","+cid+","+cid+",'"+sname+"','"+sname+"','"+zy+"',"+old_zt+",NULL)";
        stmt.executeUpdate(sql3);
        System.out.println("修改完成");
        
        
    }
    
    
    public void changStudent(Statement stmt) throws Exception {
    	System.out.println("请输入要修改的学生编号：");
        int sid = scan.nextInt();
        
        String sql = "SELECT * FROM student WHERE sid="+sid+"";
        ResultSet rs = stmt.executeQuery(sql);
        if (!rs.next()) {
            System.out.println("错误：未找到编号为 "+sid+" 的学生！");
            return;
        }
		int cid = rs.getInt("cid");
        String old_sname = rs.getString("sname");
        String old_zy = rs.getString("zy");
        int zt = rs.getInt("zt");
        System.out.println("请输入新姓名：");
        String new_sname = scan.next();
        System.out.println("请输入新专业：");
        String new_zy = scan.next();
       
        String sql2 = "UPDATE student SET sname='"+new_sname+"',zy='"+new_zy+"' WHERE sid="+sid+"";
        stmt.executeUpdate(sql2);
        
        String sql3 = "INSERT INTO student_change(sid,old_cid,new_cid,old_sname,new_sname,old_zy,new_zy,old_zt,new_zt) VALUES(" + sid + "," + cid + "," + cid + ",'" + old_sname  + "','" +new_sname + "','" + old_zy + "','" + new_zy + "'," + zt + "," + zt + ")";
        stmt.executeUpdate(sql3);
        
        System.out.println("新姓名："+ new_sname);
        System.out.println("新专业："+ new_zy);
        System.out.println("修改完成");
    }
    
    
    public void resumeStudy(Statement stmt) throws Exception {
    	System.out.println("请输入复课学生编号：");
        int sid = scan.nextInt();
    	String sql = "SELECT * FROM student WHERE sid="+sid+"";
 		ResultSet rs = stmt.executeQuery(sql);
 		if (!rs.next()) {
            System.out.println("错误：未找到编号为 "+sid+" 的学生！");
            return;
        }
 		int cid = rs.getInt("cid");
        String sname = rs.getString("sname");
        String zy = rs.getString("zy");
        int old_zt = rs.getInt("zt");
        String sql2 = "UPDATE student SET zt=1 WHERE sid="+sid+"";
        stmt.executeUpdate(sql2);
        String sql3 = "INSERT INTO student_change(sid,old_cid,new_cid,old_sname,new_sname,old_zy,old_zt,new_zt) VALUES("+sid+","+cid+","+cid+",'"+sname+"','"+sname+"','"+zy+"',"+old_zt+",1)";
        stmt.executeUpdate(sql3);
        System.out.println("复课完成");
        
        
    }
    
    
    
    public void showStudentChange(Statement stmt) throws Exception {
    	System.out.println("请输入学生编号：");
        int sid = scan.nextInt();
        String sql = "SELECT sc.id,sc.old_sname, sc.new_sname, sc.old_cid,sc.new_cid,sc.old_zy, sc.new_zy,sc.old_zt, sc.new_zt FROM student_change sc JOIN student s ON sc.sid=s.sid WHERE sc.sid="+sid+"";
        ResultSet rs = stmt.executeQuery(sql);

        System.out.println("变动ID\t旧班级\t新班级\t旧姓名\t新姓名\t旧专业\t新专业\t旧状态\t新状态");
        while (rs.next()){
        	System.out.println(rs.getInt("id") + "\t" + rs.getInt("old_cid") + "\t" + rs.getInt("new_cid") + "\t" + rs.getString("old_sname") + "\t" + rs.getString("new_sname") + "\t"
        		+ rs.getString("old_zy") + "\t" + rs.getString("new_zy") + "\t"+ rs.getInt("old_zt") + "\t" + rs.getInt("new_zt"));
        	}
    }
    
    
    public void theMostChangeStudent(Statement stmt) throws Exception {
    	String sql = "SELECT sid, COUNT(*) '变动次数' FROM student_change GROUP BY sid HAVING COUNT(*) = ( SELECT MAX(cnt) FROM (SELECT COUNT(*) cnt FROM student_change GROUP BY sid) AS temp)";
        ResultSet rs = stmt.executeQuery(sql);
        while (rs.next()) {
            System.out.println("变动最多的学生编号：" + rs.getInt("sid") + "，变动次数：" + rs.getInt("变动次数"));
        }
    }
    public void updateZy(Statement stmt) throws Exception {
    	System.out.println("请输入学生编号：");
        int sid = scan.nextInt();
        String sql = "SELECT * FROM student WHERE sid="+sid+"";
 		ResultSet rs = stmt.executeQuery(sql);
 		if (!rs.next()) {
            System.out.println("错误：未找到编号为 "+sid+" 的学生！");
            return;
        }
 		int cid = rs.getInt("cid");
        String sname = rs.getString("sname");
        String old_zy = rs.getString("zy");
        int zt = rs.getInt("zt");
        System.out.println("新专业：");
        String new_zy = scan.next();
        String sql2 = "UPDATE student SET zy='" + new_zy + "' WHERE sid="+sid+"";
        stmt.executeUpdate(sql2);
        
        String sql3 = "INSERT INTO student_change(sid,old_cid,new_cid,old_sname,new_sname,old_zy,new_zy,old_zt,new_zt) VALUES("+sid+","+cid+","+cid+",'"+sname+"','"+sname+"','"+old_zy+"','"+new_zy+"',"+zt+","+zt+")";
        stmt.executeUpdate(sql3);
        System.out.println("新专业："+ new_zy);
        System.out.println("修改完成");
    }
    
    
    public void showScore(Statement stmt) throws Exception {
    	System.out.println("请输入学生编号：");
        int sid = scan.nextInt();
        String sql = "SELECT * FROM score JOIN student ON score.sid = student.sid WHERE student.sid="+sid+"";
        ResultSet rs = stmt.executeQuery(sql);
        String sname = null;
        while (rs.next()) {
        	if (sname == null) {
        		sname = rs.getString("sname");
        		System.out.println("学生姓名：" + sname);
                System.out.println("------------------------");
        		System.out.println("科目\t成绩\t学分");
        		
        	}
            System.out.println(rs.getString("kc") + "\t" + rs.getDouble("cj") + "\t" + rs.getDouble("xf"));
        }
    }
    
    public void showsumScore(Statement stmt) throws Exception {
    	System.out.println("请输入学生编号：");
        int sid = scan.nextInt();
    	String sql = "SELECT SUM(cj) sum ,sname FROM score JOIN student ON score.sid = student.sid WHERE student.sid="+sid+"";
        ResultSet rs = stmt.executeQuery(sql);
        if (rs.next()) {
        	String sname = rs.getString("sname");
    		System.out.println("学生姓名：" + sname);
        	System.out.println("总成绩：" + rs.getDouble("sum"));
        }
    }
    
    public void showAvgScore(Statement stmt) throws Exception {
    	System.out.println("请输入学生编号：");
        int sid = scan.nextInt();
    	String sql = "SELECT AVG(cj) avg,sname FROM score JOIN student ON score.sid = student.sid WHERE student.sid="+sid+"";
        ResultSet rs = stmt.executeQuery(sql);
        if (rs.next()) {
        	String sname = rs.getString("sname");
    		System.out.println("学生姓名：" + sname);
        	System.out.println("平均成绩：" + rs.getDouble("avg"));
        }
    }
    
    public void sumPoint(Statement stmt) throws Exception {
    	System.out.println("请输入学生编号：");
        int sid = scan.nextInt();
        String sql = "SELECT cj ,xf ,sname FROM score JOIN student ON score.sid = student.sid WHERE student.sid="+sid+"";
        ResultSet rs = stmt.executeQuery(sql);
        String sname = null;
        double sum_jidian = 0;
        while (rs.next()) { 
            if (sname == null) {
                sname = rs.getString("sname");
                System.out.println("学生姓名：" + sname);
            }

            double cj = rs.getDouble("cj");
            double xf = rs.getDouble("xf");
            double jidian = (cj * xf) * 0.1;
            sum_jidian += jidian;
        }
        if (sname != null) {
            System.out.printf("总绩点:"+ sum_jidian);
        } else {
            System.out.println("错误：未找到编号为 "+sid+" 的学生！");
        }
    }
    

}
	
	
	
	
	
	
	
	
	
	
	
	
	

```

